version: "3.1"

services:
  mongo:
    image: mongo:4
    volumes:
      - ./data/mongo:/data/db

  bot:
    build: .
    command: yarn dev
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
    env_file: .env
    environment:
      MONGODB_URL: mongodb://mongo:27017/shipid
      DEBUG: shipid
    depends_on:
      - mongo
